# RTL + React + TypeScript + Vite + Vitest

ademas incluye un par de plugins para eslint relacionados a RTL (ver el package)

para ejecutar instala las dependencias, revisa el package para los run scripts y listo
